자바스크립트에서 변수 x를 정의해보자. 어떤 키워드를 사용하여 변수를 정의할 수 있을까? := let x;
const 키워드는 무엇을 정의하기 위해 사용? := 상수
숫자를 가지는 JS의 데이터 타입은? := 수치형
텍스트 데이터를 가지는 JS의 데이터 타입은? := 문자열
true와 false 값을 가지는 JS의 데이터 타입은? := 부울형
빈 값 또는 정의되지 않은 값을 가지는 JS의 데이터 타입은? := undefined
객체를 가지는 JS의 데이터 타입은? := 객체형
문자열들을 대문자로 변경 하는 메서드는? := toUpperCase()
템플릿 리터럴 문자열을 사용하는 기호는? := $
${변수명}과 같은 형식으로 변수를 삽입하는 방법은? := 템플릿 리터럴
객체형을 선언할때 사용하는 기호는 ? := {}
값과 타입이 모두 같은지 비교하는 연산자는? := ===
값이나 타입이 다른지 비교하는 연산자는? := !==
값이 같은지 비교하는 연산자는? := ==
console에 값을 출력하는 함수는? := console.log()
html 정보를 가지고 있는 객체는? := document
html 요소를 id로 선택하는 메서드는? := getElementById()
요소의 내용, 요소의 contents에 접근하는 속성은? := innerHTML
html문서에 쓸때 사용하는 메소드 := document.write();
form요소의 값, 즉, 사용자의 입력값에 접근하는 속성 := value
현재 시각을 가져오는 객체 := Date
현재 시간을 가져오려면 ? := new Date().getHours()
정해진 횟수만큼 반복하는 반복문 := for
조건이 참인 동안 반복하는 반복문 := while
함수를 정의하는 키워드 := function
자바스크립트는 웹페이지의 _____을 제어하기 위해 사용된다. := 동작
자바스크립트는 변수의 타입을 ____으로 결정한다. := 동적
자바스크립트는 _____ 위에서 실행된다. := 브라우저
자바스크립트는 ________ 언어이다. := 인터프리터
자바스크립트의 특징중 하나인 __________덕분에 변수가 동적으로 정의된다. := 동적타이핑
자바스크립트는 HTML 컨텐츠를 _____으로 변경할 수 있다. := 동적
자바스크립트를 해석하고 실행하는 주체는 ____________이다. := 브라우저
HTML 문서 안에 자바스크립트를 삽입할 때 사용하는 태그는 ____________이다. := <script>
외부 자바스크립트 파일을 불러올 때 사용하는 속성은 ____________이다. := src
변수 선언 시 값이 변하지 않도록 만들 때 사용하는 키워드는 ____________이다. := const
변수의 타입을 선언하지 않아도 되는 특징을 ____________ 타이핑이라고 한다. := 동적
페이지가 모두 로드된 후 document.write()를 실행하면 HTML 문서가 ____________ 된다. := 초기화
특정 HTML 요소에 접근하기 위해 사용하는 메서드는 document.____________() 이다. := getElementById
주어진 조건이 참인 동안 반복하는 반복문은 ____________문이다. := while
함수에서 값을 되돌려줄 때 사용하는 키워드는 ____________이다. := return
문자열 안에 변수값을 삽입할 때 사용하는 문법을 ____________ 리터럴이라고 한다. := 템플릿
사용자의 입력을 문자열로 받는 함수는 ____________이다. := prompt
사용자에게 확인/취소 버튼이 있는 창을 띄우는 함수는 ____________이다. := confirm
HTML 요소의 글자색을 변경할 때 사용하는 속성은 style.__________ 이다. := color
HTML 요소의 스타일에 접근 가능한 속성은  ____________이다. := style
반복문에서 반복을 중단할 때 사용하는 문장은 ____________이다. := break
자바스크립트에서 변수가 선언되었지만 값이 없는 상태를 나타내는 키워드는 ____________이다. := undefined
요소를 클릭할때 발생하는 이벤트는 ____________이다. := onclick
마우스를 요소의 위에 올릴때 발생하는 이벤트는 ____________이다. := onmouseover
마우스를 요소에서 뗄때 발생하는 이벤트는 ____________이다. := onmouseout
외부의 자바스크립트 파일 test.js를 불려들이려면 어떻게 해야 하는가? := <script src="test.js">
HTML 파일 안에 자바스크립트 코드를 추가하려면 어떤 태그를 사용해야 하는가? := <script>
일반적으로 자바	스크립트 코드는 HTML 문서의 어디에 위치하는가? := <body>